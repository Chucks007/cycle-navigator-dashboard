# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt

# Linting
ruff>=0.1.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Playwright for E2E tests (install browsers separately with: python -m playwright install)
playwright>=1.40.0
